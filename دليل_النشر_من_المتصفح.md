# دليل النشر من المتصفح — بلا أي أوامر

من الصفر إلى رابط يعمل. الوقت المتوقع: 10 إلى 15 دقيقة.

---

## قبل أن تبدأ

فُكّ ضغط الملف `منصة_التعليم_الأخضر_مستودع.zip`. ستجد مجلداً باسم `repo` يحوي:

```
main.ts · deno.json · README.md · .gitignore · .env.example
data/       → seed.json
src/        → db.ts · auth.ts · api.ts
static/     → index.html · style.css · app.js
tests/      → api_test.ts
.github/    → workflows/deploy.yml   (اختياري)
```

**انتبه:** المجلدان `.github` و`.gitignore` يبدآن بنقطة، وأنظمة التشغيل تخفيهما افتراضياً. لن يعطّل غيابهما شيئاً
— المنصة تعمل بدونهما. التفصيل في الخطوة 3.

---

## الخطوة 1 — إنشاء المستودع على GitHub

1. ادخل <https://github.com> وسجّل الدخول.
2. اضغط علامة **`+`** أعلى اليمين ثم **New repository**.
3. املأ:
   - **Repository name:** `green-education-platform`
   - **Description:** منصة تقييم التعليم الأخضر
   - اختر **Private** (موصى به — البيانات تخص الوزارة).
   - **لا تضع علامة** على `Add a README file` ولا على `.gitignore` ولا `license`. المستودع يجب أن يكون فارغاً
     تماماً.
4. اضغط **Create repository**.

ستظهر صفحة فيها تعليمات بأوامر — تجاهلها، وابحث عن السطر:

> **uploading an existing file**

اضغط عليه. (أو ادخل مباشرة على `https://github.com/<حسابك>/green-education-platform/upload/main`)

---

## الخطوة 2 — رفع الملفات

في صفحة الرفع سترى مساحة مكتوب فيها **Drag files here**.

1. افتح مجلد `repo` على جهازك.
2. حدّد **كل ما بداخله** (وليس المجلد نفسه): `Ctrl+A` على ويندوز أو `Cmd+A` على ماك.
3. اسحبها ودعها في مساحة الرفع في المتصفح.
4. انتظر ظهور أسماء الملفات كاملة — يجب أن ترى `main.ts` و`deno.json` والمجلدات `data` و`src` و`static`
   و`tests`.
5. في خانة **Commit changes** أسفل الصفحة اكتب: `الإصدار الأول`
6. اضغط **Commit changes**.

### تحقّق قبل المتابعة

افتح صفحة المستودع. يجب أن ترى **على الأقل** هذه العناصر:

| العنصر                         | ضروري؟                              |
| ------------------------------ | ----------------------------------- |
| `main.ts`                      | **نعم** — بدونه لن يعمل شيء         |
| `deno.json`                    | **نعم** — يحوي إعداد قاعدة البيانات |
| مجلد `data` بداخله `seed.json` | **نعم** — المؤسسات والحسابات        |
| مجلد `src` بداخله 3 ملفات      | **نعم**                             |
| مجلد `static` بداخله 3 ملفات   | **نعم**                             |
| مجلد `tests`                   | لا — للفحص فقط                      |
| `.gitignore` و`.github`        | لا — اختياريان                      |

إن نقص أحد الضروريات، اضغط **Add file ← Upload files** وارفعه وحده.

---

## الخطوة 3 — المجلدات المخفية (تخطَّ هذه الخطوة إن أردت)

إن لم يظهر `.gitignore` أو `.github` بعد الرفع فلا مشكلة، المنصة تعمل بدونهما. وإن أردت إضافتهما من المتصفح:

1. في المستودع اضغط **Add file ← Create new file**.
2. اكتب في خانة الاسم: `.gitignore` ثم الصق فيه محتوى الملف من مجلد `repo`.
3. اضغط **Commit changes**.

ولملف الفحص الآلي: كرّر الخطوة واكتب في خانة الاسم `.github/workflows/deploy.yml` — اكتب المسار كاملاً وستُنشأ
المجلدات تلقائياً.

---

## الخطوة 4 — الربط بـ Deno Deploy

1. ادخل <https://dash.deno.com>.
2. اضغط **Sign in with GitHub** ثم **Authorize**.
3. اضغط **New Project**.
4. اختر **Deploy from GitHub repository**.
5. من القائمة اختر مستودع `green-education-platform`.
   - إن لم يظهر، اضغط **Configure GitHub App** وامنح الوصول للمستودع ثم ارجع.
6. اضبط:
   - **Branch:** `main`
   - **Entrypoint:** `main.ts`
   - **Build step:** اتركه فارغاً — المشروع لا يحتاج بناءً.
7. اضغط **Deploy Project**.

---

## الخطوة 5 — كلمة المرور الابتدائية

**هذه أهم خطوة. نفّذها قبل فتح الرابط.**

1. في صفحة المشروع اضغط **Settings** ثم **Environment Variables**.
2. اضغط **Add Variable**:
   - **Key:** `SEED_PASSWORD`
   - **Value:** كلمة مرور قوية من اختيارك (12 محرفاً فأكثر)
3. احفظ.
4. ارجع إلى **Deployments** واضغط **Redeploy** على آخر نشر.

**لا تضف `KV_PATH`** — قاعدة البيانات تُدار تلقائياً على Deno Deploy.

إن فتحت الرابط قبل ضبط المتغير، ستُولَّد كلمة مرور عشوائية وتُطبع في سجل التشغيل (**Logs** في لوحة المشروع). ابحث
فيه عن سطر «كلمة المرور الابتدائية».

---

## الخطوة 6 — التجربة

افتح الرابط الذي منحك إياه Deno Deploy، وسيكون بالشكل:

```
https://green-education-platform.deno.dev
```

جرّب الدخول بثلاثة حسابات لترى الفروق:

| الحساب | الدور             | ما يراه                         |
| ------ | ----------------- | ------------------------------- |
| `TECH` | الحساب الفني      | كل شيء + الحسابات + سجل التدقيق |
| `Z1-L` | رئيس فريق منطقة 1 | فريقه · أعلى 10 · قصص النجاح    |
| `Z1-1` | عضو فريق تقييم    | مؤسساته المسندة فقط             |

كلمة المرور لكل الحسابات هي قيمة `SEED_PASSWORD` التي ضبطتها. عند أول دخول ستُطالَب بتغييرها.

---

## الخطوة 7 — تسليم الحسابات للفرق

من حساب `TECH` ادخل شاشة **الحساب الفني**، وستجد جدول الحسابات الـ37 وأمام كل حساب زر **إعادة تعيين**. استخدمه
لتعيين كلمة مرور مبدئية لكل عضو، وسلّمها له ليغيّرها عند أول دخول.

**قائمة الحسابات:**

| الفريق        | أعضاء التقييم   | رئيس الفريق |
| ------------- | --------------- | ----------- |
| منطقة 1       | `Z1-1` … `Z1-5` | `Z1-L`      |
| منطقة 2       | `Z2-1` … `Z2-5` | `Z2-L`      |
| منطقة 3       | `Z3-1` … `Z3-5` | `Z3-L`      |
| منطقة 4       | `Z4-1` … `Z4-5` | `Z4-L`      |
| التعليم الخاص | `PR-1` … `PR-5` | `PR-L`      |
| رياض الأطفال  | `KG-1` … `KG-5` | `KG-L`      |

---

## التعديل لاحقاً — أيضاً من المتصفح

لتغيير أي ملف: افتحه في GitHub، اضغط أيقونة **القلم**، عدّل، ثم **Commit changes**. Deno Deploy ينشر التعديل
تلقائياً خلال ثوانٍ.

---

## حلّ المشكلات

| العَرَض                                  | السبب والحل                                                                                                                    |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| الصفحة بيضاء أو خطأ 500                | افتح **Logs** في لوحة المشروع. الغالب أن `main.ts` أو `deno.json` لم يُرفع في جذر المستودع.                                     |
| «بيانات الدخول غير صحيحة» رغم صحتها    | لم يُضبط `SEED_PASSWORD` قبل أول تشغيل. اضبطه ثم **Redeploy**، وإن استمر فالحسابات أُنشئت بكلمة عشوائية — ابحث عنها في **Logs**. |
| ملفات التنسيق لا تظهر (صفحة بلا ألوان) | مجلد `static` لم يُرفع كاملاً. تأكد أن بداخله الملفات الثلاثة.                                                                   |
| «Entrypoint not found»                 | تأكد أن `main.ts` في **جذر** المستودع لا داخل مجلد `repo`.                                                                     |
| المستودع فيه مجلد اسمه `repo`          | رفعت المجلد نفسه بدل محتواه. احذف الملفات وأعد الرفع بتحديد المحتوى.                                                           |

---

## تنبيهات قبل الاستخدام الرسمي

- اجعل المستودع **Private** — بيانات المؤسسات تخص الوزارة.
- غيّر كلمات مرور كل الحسابات فور التسليم.
- Deno Deploy يستضيف خارج البحرين. راجع متطلبات سيادة البيانات مع الجهة المختصة قبل إدخال بيانات فعلية.
- اضبط نسخاً احتياطياً دورياً وجرّب الاسترجاع — التقييم السنوي لا يُعاد.
